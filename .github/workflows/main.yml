name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # Updated to v4 for better performance and features | Was v3 before

      - name: Display Success Message
        run: echo "Build Successful!"
